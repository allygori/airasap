# Historical AI Agents Guide (non-canonical)

Use the repository-root `AGENTS.md` as the only repository-wide instruction
file. The active detailed guides are listed in `.agents/README.md`.

This file is retained only as a compatibility pointer for tools or scripts that
look for `.agents/AGENTS.md`. It intentionally contains no architecture or
coding rules so it cannot conflict with the current documentation.


<!-- End of compatibility pointer. -->
