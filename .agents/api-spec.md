# Deprecated API specification

This file is retained only as a historical pointer. Its `/api/products`,
`/api/layout`, and `/api/experiments` examples are not the current API.

Use the repository-root `AGENTS.md` and `.agents/api-and-data-access.md`.
For active routes, inspect `app/api/` and its tests.
