# Deprecated folder map

This folder map described planned `/engines`, `/jobs`, and `/config` folders
that are not the current implementation. Use `.agents/ARCHITECTURE.md` for the active map and `.agents/module-boundaries.md` for domain placement.
