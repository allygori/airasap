# Historical layout-system note

The generic JSON-driven landing-page/layout engine described here is not implemented in the current codebase. Treat this file as `[TARGET]` product history. Do not add layout models, section registries, or caching behavior based only on this note.

If this capability is restarted, define its schema, module boundary, security model, rendering contract, and tests in a new architecture decision first.
